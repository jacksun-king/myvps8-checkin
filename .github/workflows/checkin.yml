name: VPS8 每日签到

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  checkin:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 安装系统依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 ffmpeg \
          libxkbcommon0 libxcomposite1 libxdamage1 libxrandr2 libgbm1 libasound2t64 libgl1 \
          fonts-noto-cjk fonts-noto-color-emoji

      - name: 安装 Python 依赖
        run: pip install seleniumbase requests loguru SpeechRecognition pydub

      - name: 执行签到
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
          AI_BASE_URL: ${{ secrets.AI_BASE_URL }}
          AI_MODEL_NAME: ${{ secrets.AI_MODEL_NAME }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          MY_CHAT_ID: ${{ secrets.MY_CHAT_ID }}
          VPS8_EMAIL: ${{ secrets.VPS8_EMAIL }}
          VPS8_PASSWORD: ${{ secrets.VPS8_PASSWORD }}
          VPS8_COOKIES: ${{ secrets.VPS8_COOKIES }}
          VPS8_API_KEY: ${{ secrets.VPS8_API_KEY }}
        run: python vps8_checkin.py

      - name: 上传截图
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vps8-screenshots
          path: output/vps8/
          if-no-files-found: ignore
